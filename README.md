# 📬📅 MCP Email Calendar Server

**Готовый к продакшену MCP-сервер**, объединяющий **почту** и **календарь**  в единый контекстно-осознанный интерфейс для LLM. Построен с использованием **[FastMCP 2.0](https://gofastmcp.com)** — стандартного фреймворка для разработки MCP-приложений.

> ⚡ Подключите вашу LLM к реальным рабочим процессам: читайте письма, создавайте события в календаре — всё через Model Context Protocol.

---

## ✨ Возможности

- **📧 Работа с электронной почтой**  
  – Отправка писем нескольким получателям (`to`, `cc`, `bcc`)  
  – Получение, поиск и фильтрация писем (по отправителю, дате, статусу прочтения)

- **📅 Календарь и задачи (CalDAV)**  
  – Создание событий и задач (`VTODO`)  
  – Просмотр просроченных, сегодняшних или предстоящих событий  
  – Полная асинхронная поддержка через `aiocaldav`

- **🔐 Безопасность «из коробки»**  
  – Нет открытых внешних зависимостей

- **🧩 MCP-ориентированная архитектура**  
  – Инструменты (Tools), Ресурсы (Resources) и Промпты (Prompts)  
  – Динамические URI-шаблоны (`events://today`)  
  – Поддержка контекстного запроса к LLM через `ctx.sample()`

- **📦 Модульная и расширяемая структура**  
  – Чёткое разделение: модули `email` и `calendar`  
  – Легко добавить контакты, файлы или кастомную логику

---
## 🎯 Используем всю мошь [cloud.ru](https://cloud.ru)

### 1. Быстрый старт сервиса  [Artifact Registry](https://cloud.ru/docs/artifact-registry-evolution/ug/topics/quickstart)

- создание реестра
- загрузка Docker-образ в репозиторий

### 2. Быстрый старт сервиса [AI Agents](https://cloud.ru/docs/ai-agents/ug/topics/quickstart)

- создание МСР-сервера из Artifact Registry
- создание агента


## 🚀 Быстрый старт (локально)

### 1. Установка через `uv` (рекомендуется)

```bash
uv sync --no-editable
```

### 2. Настройка .env

#### Почта (IMAP/SMTP)
EMAIL_ADDRESS=ваша_почта@yandex.ru \
EMAIL_PASSWORD=ваш_пароль_приложения \
SMTP_HOST=smtp.yandex.ru \
SMTP_PORT=465 \
IMAP_HOST=imap.yandex.ru \
IMAP_PORT=993

#### CalDAV (Nextcloud, iCloud, FastMail и др.)
CALDAV_URL=https://caldav.yandex.ru \
CALDAV_USERNAME=ваша_почта@yandex.ru \
CALDAV_PASSWORD=ваш_пароль \
CALDAV_CALENDAR_NAME=имя_календаря

> ⚠️ Документация по созданию [Пароли приложений](https://yandex.ru/support/id/ru/authorization/app-passwords) провайдера электронной почты
и календаря yandex


#### Безопасность
MCP_API_KEY=ваш_надёжный_секретный_ключ

> 💡 Никогда не коммитьте .env в Git!

### 3. Запуск сервера

#### CLI-режим (stdio) — идеален для локальных LLM-инструментов
```bash
fastmcp run main.py
```

#### HTTP/SSE-режим — для веб-клиентов или удалённого доступа
```bash
uv run smain.py
```

> Сервер автоматически определяет транспорт при запуске через `fastmcp run`

## 🔌 Примеры использования
#### Из любого MCP-совместимого клиента:

```python
from fastmcp import Client

async with Client("http://localhost:8000/mcp") as client:
    # Отправить письмо
    await client.call_tool("email_send_email", {
        "to": [{"email": "friend@example.com"}],
        "subject": "Привет от MCP!",
        "body": "Это письмо отправлено через Model Context Protocol."
    })

    # Получить события на сегодня
    events = await client.read_resource("calendar://today")
```

## Основные ресурсы

URI|Описание|
|:---|:---
|calendar://today|События, запланированные на сегодня|
|emails://unread|Последние 5 непрочитанных писем|

## Основные инструменты

-  email_send_email
-  email_get_email
-  email_search_by_sender
-  calendar_create_event

## 🧱 Структура проекта

```
src/
├── main.py          # Точка входа
├── config.py          # Загрузка и валидация .env
└── core/
    ├── email/       # Управление почтой
        ├── __init__.py  # MCP сервер почты
        ├── utils.py # утилиты 
        ├── prompts.py # промты 
        ├── resources.py # ресурсы
        └── tools.py # инструменты
    └── calendar/    # Управление календарём
        ├── __init__.py  # MCP сервер календаря
        ├── prompts.py # промты 
        ├── resources.py # ресурсы
        └── tools.py # инструменты
tests/
├── __init__.py 
└── test_mcp_server.py # тесты МСР сервера
```

## 📚 Документация

- [Официальная документация FastMCP 2.0](https://gofastmcp.com/)
- [Спецификация протокола MCP](https://modelcontextprotocol.io/)